[package]
name = "queries-embed"
version = "0.13.0"
authors = ["Kevin Lefèvre <kevin.lefevre@snips.ai>", "Thibaut Lorrain <thibaut.lorrain@snips.ai>"]

[dependencies]
queries-core = { path = "../queries-core" }
error-chain = { version = "0.10", default-features = false }
lazy_static = "0.2"
libc = "0.2"
serde_json = "1.0"

[lib]
crate-type = ["staticlib", "cdylib"]
